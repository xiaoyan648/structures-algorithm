### 📅 第6周：数组/字符串进阶专项刷题表

| 天数 | 聚焦考点 | 题目编号 | 题目名称 (LeetCode) | 核心解法/备注 |
| :--- | :--- | :--- | :--- | :--- |
| **Day 1** | **滑动窗口**<br>(变长窗口) | **209** | [Minimum Size Subarray Sum](https://leetcode.cn/problems/minimum-size-subarray-sum/) <br> (长度最小的子数组) | **入门题**。窗口伸缩模版，找满足条件的最小窗口。 |
| | | **3** | [Longest Substring Without Repeating Characters](https://leetcode.cn/problems/longest-substring-without-repeating-characters/) <br> (无重复字符的最长子串) | **必考题**。哈希表+滑动窗口，处理窗口内的重复元素。 |
| **Day 2** | **滑动窗口**<br>(定长/计数) | **438** | [Find All Anagrams in a String](https://leetcode.cn/problems/find-all-anagrams-in-a-string/) <br> (找到字符串中所有字母异位词) | **定长窗口**。利用数组/哈希表统计字符频率。 |
| | | **567** | [Permutation in String](https://leetcode.cn/problems/permutation-in-string/) <br> (字符串的排列) | 与438类似，判断窗口内字符计数是否匹配。 |
| **Day 3** | **前缀和**<br>(哈希优化) | **560** | [Subarray Sum Equals K](https://leetcode.cn/problems/subarray-sum-equals-k/) <br> (和为 K 的子数组) | **核心题**。前缀和 + 哈希表(Two Sum思想的升级版)。 |
| | | **523** | [Continuous Subarray Sum](https://leetcode.cn/problems/continuous-subarray-sum/) <br> (连续的子数组和) | 前缀和 + 哈希表 + 同余定理。 |
| **Day 4** | **混合强化**<br>(乘积/整除) | **713** | [Subarray Product Less Than K](https://leetcode.cn/problems/subarray-product-less-than-k/) <br> (乘积小于 K 的子数组) | 滑动窗口。注意处理窗口内元素个数的计算逻辑。 |
| | | **974** | [Subarray Sums Divisible by K](https://leetcode.cn/problems/subarray-sums-divisible-by-k/) <br> (和可被 K 整除的子数组) | 前缀和 + 同余。与523类似，但求的是计数。 |
| **Day 5** | **字符串匹配**<br>(KMP/其他) | **28** | [Find the Index of the First Occurrence in a String](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/) <br> (找出字符串中第一个匹配项的下标) | **KMP模版题**。理解 `next` 数组的构建。 |
| | | **459** | [Repeated Substring Pattern](https://leetcode.cn/problems/repeated-substring-pattern/) <br> (重复的子字符串) | KMP的 `next` 数组性质应用，或者简单的字符串移位法。 |

---

### 💡 核心考点总结（备忘）

1.  **滑动窗口 (Sliding Window)**
    *   **适用场景**：子数组/子串问题（连续区间），求最值或计数。
    *   **关键点**：`right` 主动右移扩大窗口，`left` 被动右移缩小窗口（满足/不满足条件时）。
    *   **区分**：
        *   求“最小”窗口：先扩张直到满足条件，再收缩（如 LC 209）。
        *   求“最大”窗口：扩张时维护条件，不满足则收缩（如 LC 3）。

2.  **前缀和 (Prefix Sum)**
    *   **适用场景**：子数组求和问题，尤其是需要 $O(1)$ 查询区间和。
    *   **高阶技巧**：**前缀和 + 哈希表**。
        *   公式：`presum[j] - presum[i] = k`  =>  `presum[i] = presum[j] - k`。
        *   在遍历 `j` 时，查找哈希表中是否存在 `presum[j] - k`。

3.  **KMP 算法**
    *   **核心**：利用已匹配的信息，避免指针回退。
    *   **重点**：理解 `next[i]` (或 `pi[i]`) 的含义——最长相等前后缀的长度。

---